# Email Configuration for Contact Form
# These are optional - the form will work without them but won't send actual emails

# SMTP Configuration (for Gmail, use smtp.gmail.com)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Email credentials
SMTP_USER=conniexu444@gmail.com
SMTP_PASS=your-app-password-here

# Recipient email (where contact form messages will be sent)
TO_EMAIL=conniexu444@gmail.com

# Note: For Gmail, you'll need to:
# 1. Enable 2-factor authentication on your Google account
# 2. Go to Google Account → Security → 2-Step Verification → App passwords
# 3. Generate an "App Password" for "Mail" and use that for SMTP_PASS
# 4. Use the 16-character app password (no spaces) instead of your regular password